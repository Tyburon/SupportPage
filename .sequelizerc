const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'config', 'database.ts'),
  'migrations-path': path.resolve(__dirname, 'src', 'shared', 'infra', 'sequelize', 'migrations'),
};
